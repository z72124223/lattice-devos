[package]
name = "lattice-orchestrator"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
rust-version.workspace = true

[dependencies]
lattice-codebase-memory = { path = "../lattice-codebase-memory" }
lattice-contracts = { path = "../lattice-contracts" }
lattice-foreman-state = { path = "../lattice-foreman-state" }
lattice-ports = { path = "../lattice-ports" }
lattice-task-domain = { path = "../lattice-task-domain" }
lattice-writer-lease = { path = "../lattice-writer-lease" }
serde_json = "=1.0.151"

[dev-dependencies]
lattice-task-ledger = { path = "../lattice-task-ledger" }

[lints]
workspace = true
