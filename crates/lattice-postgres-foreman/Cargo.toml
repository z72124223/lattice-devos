[package]
name = "lattice-postgres-foreman"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
rust-version.workspace = true

[dependencies]
lattice-approval-verifier = { path = "../lattice-approval-verifier" }
lattice-artifact-store = { path = "../lattice-artifact-store" }
lattice-contracts = { path = "../lattice-contracts" }
lattice-foreman-state = { path = "../lattice-foreman-state" }
lattice-task-ledger = { path = "../lattice-task-ledger" }
postgres = { version = "=0.19.14", default-features = false }
serde_json = "=1.0.151"
sha2 = "=0.11.0"

[dev-dependencies]
lattice-postgres-store = { path = "../lattice-postgres-store" }
lattice-postgres-writer-lease = { path = "../lattice-postgres-writer-lease" }
lattice-project-registry = { path = "../lattice-project-registry" }
lattice-writer-lease = { path = "../lattice-writer-lease" }

[lints]
workspace = true
