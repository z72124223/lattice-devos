[package]
name = "lattice-postgres-store"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
rust-version.workspace = true

[dependencies]
lattice-cjson = { path = "../lattice-cjson" }
lattice-contracts = { path = "../lattice-contracts" }
lattice-ports = { path = "../lattice-ports" }
lattice-project-registry = { path = "../lattice-project-registry" }
lattice-task-ledger = { path = "../lattice-task-ledger" }
postgres = { version = "=0.19.14", default-features = false, features = ["with-serde_json-1"] }
serde_json = "=1.0.151"
sha2 = "=0.11.0"

[lints]
workspace = true
