{
  "name": "lattice-devos",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first autonomous AI development platform.",
  "type": "module",
  "engines": {
    "node": ">=24.15.0"
  },
  "scripts": {
    "check": "node scripts/check-project.mjs",
    "test": "node --test \"test/*.test.js\"",
    "test:domain": "node --test test/task-domain.test.js",
    "verify": "npm run check && npm test"
  }
}
